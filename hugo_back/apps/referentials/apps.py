from django.apps import AppConfig


class ReferentialsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.referentials"
    label = "referentials"
    verbose_name = "Referentials (RNCP/RS, overlays)"
