/** Libellés de version plateforme — unique source pour l’UI. */
export const PLATFORM_VERSION = 'Hugo 2.0'

export const PLATFORM_FOOTER_LABEL = `${PLATFORM_VERSION} — Lucia`

export const PLATFORM_VERSION_ADMIN_LINE = `Version de la plateforme : ${PLATFORM_VERSION}`
